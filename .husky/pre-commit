# npm run lint
# Disabled: ESLint has 164 errors (mostly pre-existing) that need to be fixed separately
# Lint-staged runs in apps/frontend/.husky/pre-commit for staged files only
echo "Skipping root lint check - using lint-staged in apps/frontend"
