import React from 'react';

/**
 * Composant de monitoring des performances Web Vitals
 * Désactivé temporairement pour éviter les erreurs de build
 */
export const PerformanceMonitor: React.FC = () => {
  // Composant désactivé temporairement
  return null;
};